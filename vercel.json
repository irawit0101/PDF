{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "rootDirectory": "frontend",
  "env": {
    "NODE_ENV": "production"
  },
  "functions": {
    "api/**/*.js": {
      "runtime": "nodejs18.x"
    }
  }
}
